# QYAutoSeeder

A seeder for QingYing PT, which automatically fetch 0day resources from given sites, download them, get their media information and post them to QingYing.

*Notice*: Package `transmissionrpc` is needed. You can install it using ```pip install transmissionrpc``` or ```easy_install transmissionrpc```.